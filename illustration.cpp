//we need to tell git to track the file before we can save it.(both for a new file and for changes made to an existing file)
//using git add . (to track all the listed files) or git add <filename> to track that specific file/folder.
//to commit changed to git use: git commit -m "(for main message, it's compulsory)" -m ""(for extended description)
//until now we have only saved our changes locally, the commit still isn't live yet.

#include<bits/stdc++.h>
using namespace std;
int main(){
    cout<<"hello world.";
    return 0;
}